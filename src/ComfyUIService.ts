/**
 * ComfyUI integration (placeholder).
 *
 * Future work: this module will submit prompts to a local ComfyUI instance, e.g.
 * POST http://127.0.0.1:8188/prompt
 * and poll for completed images, then register them with AssetManager for in-game use.
 *
 * Intentionally empty — no network calls yet.
 */
export class ComfyUIService {}
